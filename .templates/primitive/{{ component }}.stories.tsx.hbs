import { {{ component }}, ThemingProps } from "@chakra-ui/react";
import { getThemingArgTypes } from "@chakra-ui/storybook-addon";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import { rawTheme } from "../../theme";

export default {
  title: "styles/{{ component }}",
  component: {{ component }},
  argTypes: getThemingArgTypes(rawTheme, "{{ component }}"),
} as ComponentMeta<typeof {{ component }}>;

const {{ component }}Story = (props: ThemingProps<"{{ component }}">) => {
  return <{{ component }} {...props} />;
};

const Story: ComponentStory<typeof {{ component }}Story> = {{ component }}Story;
export const Playground = Story.bind({});
Playground.args = {};
